name: CI

on:
  push:
    branches:
      - '**'
    tags:
      - 'v*'
  pull_request:

jobs:
  test:
    name: Test x86_64-unknown-linux-gnu
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false
      matrix:
        features:
        # base features
        - ""
        - "std"
        - "ureq"
        - "tokio"
        - "tokio-hyper"
        - "stream-proto"
        - "stream-c"
        - "openssl"
        - "rustcrypto"
        # common feature combinations
        - "ureq openssl stream-c"
        - "ureq rustcrypto std"
        - "ureq std openssl stream-c rustcrypto std tokio tokio-hyper"
  
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: nightly-2026-02-13
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2

      # Install gstreamer
      - name: Setup GStreamer
        id:   setup_gstreamer
        uses: blinemedical/setup-gstreamer@v1.4.0
        with:
          version: '1.19.90'
          arch: 'x86'

      # Format check
      - name: Rustfmt
        run: cargo fmt --all -- --check

      # Clippy
      - name: Clippy
        run: cargo clippy --all-targets --no-default-features --features "${{ matrix.features }}" -- -D warnings

      # Binstall
      - name: Install cargo-binstall
        uses: cargo-bins/cargo-binstall@main

      # Cargo Shear
      - name: Install cargo-shear
        run: cargo binstall --force --no-confirm cargo-shear
      - name: Cargo Shear
        run: cargo shear --expand
  
      # Test
      - name: Test
        run: cargo test --workspace --locked --no-default-features --features "${{ matrix.features }}"
