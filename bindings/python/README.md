
# `moonlight-common-rust` Bindings for Python

This is an example of how to generate bindings using [UniFFI](https://github.com/mozilla/uniffi-rs) to `moonlight-common-rust` for python.
It is mostly used for testing.